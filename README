Microcontroller-backed hydrolic avian repelent device service

contributors: ggrise

Getting started:

  Install brew

  Get Node
  $ brew install node

  Install npm (node package manager)
  $ curl http://npmjs.org/install.sh | sh

  Use npm to install squirt dependencies
  $ npm install express
  $ npm install serialport
  $ npm install chain-gang

  To run the server:
  $ export NODE_PATH="/usr/local/lib/node"
  $ node node/squirt.js


